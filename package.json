{
  "name": "zarr-maplibre",
  "version": "0.0.1",
  "description": "MapLibre GL custom layer for rendering Zarr datasets (extracted from zarr-cesium).",
  "license": "MIT",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "prettier": "@carbonplan/prettier",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "peerDependencies": {
    "maplibre-gl": "*"
  },
  "dependencies": {
    "zarrita": "^0.5.4"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.6.3",
    "prettier": "^2.2.1",
    "@carbonplan/prettier": "^1.2.0"
  }
}
